# 沟通牌结算逻辑验证报告

> 本文档既是单机版 `SettlementCalculator` 的验证用例，也定义了**多人联网服务端的结算公式**。
> 服务端 `ServerGameManager.checkGameEnd` / `computeAllFinishedScores` 实现与本文一致；
> 客户端 `SettlementCalculator.kt` 与 `SettlementCalculatorTest.kt` 的 15 个用例同时覆盖两端逻辑。

## 结算规则

### 触发方式1：全队走完
```
走完队得分 = 本队已走完玩家已收分 + 对方未走完玩家已收分 + 对方未走完玩家手牌分
未走完队得分 = 本队已走完玩家已收分（无人走完则为0）
```

### 触发方式2：提前结算（已收分≥200）
```
该队立即胜出
双方得分 = 各自已走完玩家已收分
```

---

## 用例验证

### 用例1 ✓
**场景**: A1(100), B1(50), A2(20), A3(30), B2手牌(120), B3手牌(80)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (100+20+30) + 0 + (120+80) | **350** |
| B队 | B1走完 | 50 | **50** |

验证: 350 + 50 = 400 ✓

---

### 用例2 ✓
**场景**: A队全员走完(80+70+50), B队无人走完(已收100+60+40)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (80+70+50) + (100+60+40) + 0 | **400** |
| B队 | 无人走完 | 0 | **0** |

验证: 400 + 0 = 400 ✓

---

### 用例3 ✓
**场景**: B队全员走完(150+100+50), A队无人走完(已收30+40+30)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 无人走完 | 0 | **0** |
| B队 | 全员走完 | (150+100+50) + (30+40+30) + 0 | **400** |

验证: 0 + 400 = 400 ✓

---

### 用例4 ✓
**场景**: A3走完时触发，B3(40)未走完

走完顺序: A1(120) → B1(80) → A2(50) → B2(80) → **A3(30)** → ~~B3(40)~~

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (120+50+30) + 0 + 40 | **240** |
| B队 | B1,B2走完 | 80+80 | **160** |

验证: 240 + 160 = 400 ✓

---

### 用例5 ✓
**场景**: A队全员走完, B2已收60, B3已收40(未走完)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (90+60+50) + (60+40) + 0 | **300** |
| B队 | B1走完 | 100 | **100** |

验证: 300 + 100 = 400 ✓

---

### 用例6 ✓
**场景**: A队全员走完, B3手牌(50)未走完

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (110+40+30) + 0 + 50 | **230** |
| B队 | B1,B2走完 | 90+80 | **170** |

验证: 230 + 170 = 400 ✓

---

### 用例7 ✓
**场景**: A队全员走完(50+30+20), B队无人走完(已收150+100+50)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (50+30+20) + (150+100+50) + 0 | **400** |
| B队 | 无人走完 | 0 | **0** |

验证: 400 + 0 = 400 ✓

---

### 用例8 ✓
**场景**: B队全员走完, A2已收40, A3已收40(未走完)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | A1走完 | 120 | **120** |
| B队 | 全员走完 | (80+70+50) + (40+40) + 0 | **280** |

验证: 120 + 280 = 400 ✓

---

### 用例9 ✓ (提前结算)
**场景**: A1(110) + A2(90) = 200分，提前结算触发

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | A1,A2走完(≥200) | 110+90 | **200** |
| B队 | 无人走完 | 0 | **0** |

**注意**: 提前结算时总分可能≠400

---

### 用例10 ✓
**场景**: A3走完时触发，B3(40)未走完

走完顺序: A1(105) → B1(95) → A2(55) → B2(60) → **A3(45)** → ~~B3(40)~~

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (105+55+45) + 0 + 40 | **245** |
| B队 | B1,B2走完 | 95+60 | **155** |

验证: 245 + 155 = 400 ✓

---

### 用例11 ✓
**场景**: A队全员走完, B1已收70, B2已收60, B3手牌60(全未走完)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (100+80+30) + (70+60) + 60 | **400** |
| B队 | 无人走完 | 0 | **0** |

验证: 400 + 0 = 400 ✓

---

### 用例12 ✓
**场景**: A1(180)独揽大分, B队无人走完(已收100+60+40)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (180+10+10) + (100+60+40) + 0 | **400** |
| B队 | 无人走完 | 0 | **0** |

验证: 400 + 0 = 400 ✓

---

### 用例13 ✓
**场景**: B队全员走完, A队无人走完(A1已收190)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 无人走完 | 0 | **0** |
| B队 | 全员走完 | (60+50+40) + (190+30+30) + 0 | **400** |

验证: 0 + 400 = 400 ✓

---

### 用例14 ✓ (极端速度流)
**场景**: A队0分全走完, B队无人走完(已收200+100+100)

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (0+0+0) + (200+100+100) + 0 | **400** |
| B队 | 无人走完 | 0 | **0** |

验证: 400 + 0 = 400 ✓

---

### 用例15 ✓
**场景**: A3走完时触发，B3(0)未走完

走完顺序: A1(120) → B1(100) → A2(60) → B2(80) → **A3(40)** → ~~B3(0)~~

| 队伍 | 状态 | 计算过程 | 得分 |
|------|------|----------|------|
| A队 | 全员走完 | (120+60+40) + 0 + 0 | **220** |
| B队 | B1,B2走完 | 100+80 | **180** |

验证: 220 + 180 = 400 ✓

---

## 验证总结

| 用例 | A队得分 | B队得分 | 总分 | 触发方式 | 胜方 | 状态 |
|------|---------|---------|------|----------|------|------|
| 1 | 350 | 50 | 400 | 全队走完 | A队 | ✓ |
| 2 | 400 | 0 | 400 | 全队走完 | A队 | ✓ |
| 3 | 0 | 400 | 400 | 全队走完 | B队 | ✓ |
| 4 | 240 | 160 | 400 | 全队走完 | A队 | ✓ |
| 5 | 300 | 100 | 400 | 全队走完 | A队 | ✓ |
| 6 | 230 | 170 | 400 | 全队走完 | A队 | ✓ |
| 7 | 400 | 0 | 400 | 全队走完 | A队 | ✓ |
| 8 | 120 | 280 | 400 | 全队走完 | B队 | ✓ |
| 9 | 200 | 0 | 200 | 提前结算 | A队 | ✓ |
| 10 | 245 | 155 | 400 | 全队走完 | A队 | ✓ |
| 11 | 400 | 0 | 400 | 全队走完 | A队 | ✓ |
| 12 | 400 | 0 | 400 | 全队走完 | A队 | ✓ |
| 13 | 0 | 400 | 400 | 全队走完 | B队 | ✓ |
| 14 | 400 | 0 | 400 | 全队走完 | A队 | ✓ |
| 15 | 220 | 180 | 400 | 全队走完 | A队 | ✓ |

**结论**: 15/15 用例全部通过验证 ✓

---

## 代码验证

### 单机
- `apps/android/src/main/java/.../engine/SettlementCalculator.kt` - 独立的结算计算器
- `apps/android/src/test/.../engine/SettlementCalculatorTest.kt` - 单元测试（15 个用例）
- `apps/android/src/main/java/.../engine/SettlementVerification.kt` - 验证程序（可直接运行）

关键代码：
```kotlin
// 全队走完时的得分计算
if (teamA.allFinished) {
    teamAScore = teamA.finishedPlayersScore +      // 本队已收分
                 teamB.unfinishedCollectedScore +   // 对方未走完已收分
                 teamB.unfinishedHandScore          // 对方未走完手牌分
    teamBScore = teamB.finishedPlayersScore        // 对方已走完已收分
}
```

### 服务端（联网模式）
- `server/src/main/kotlin/.../ServerGameManager.kt`
  - `state.playerScores: MutableMap<Int, Int>` 追踪每玩家"已收"
  - `handleRoundEnd` 在累加队伍分时同时累加到 `state.playerScores[winnerId]`
  - `getStateForPlayer` 返回 `collectedScore = state.playerScores[seat] ?: 0`
  - `checkGameEnd` 与 `computeAllFinishedScores` 使用与上面**完全相同**的公式

关键代码：
```kotlin
private fun computeAllFinishedScores(
    state: ServerGameState,
    winner: List<ServerPlayer>,
    loser: List<ServerPlayer>
): Pair<Int, Int> {
    val winnerScore = winner.sumOf { state.playerScores[it.seatIndex] ?: 0 } +
        loser.filter { state.hands[it.seatIndex]?.isNotEmpty() == true }
            .sumOf { p ->
                (state.playerScores[p.seatIndex] ?: 0) +
                (state.hands[p.seatIndex]?.sumOf { c -> getCardScore(c) } ?: 0)
            }
    val loserScore = loser
        .filter { state.hands[it.seatIndex]?.isEmpty() == true }
        .sumOf { state.playerScores[it.seatIndex] ?: 0 }
    return Pair(winnerScore, loserScore)
}
```

> 单机与联网两套实现共用同一组用例，行为应保持一致。如发现联网模式与上述用例任一不符，按 bug 处理。
